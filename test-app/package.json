{
  "name": "test-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "generate-fixtures": "shx rm -rf src/app.m.css.d.ts && shx rm -rf fixtures/output && shx rm -rf output && dojo build && shx rm -rf src/app.m.css.d.ts && dojo build --mode dev && shx rm -rf src/app.m.css.d.ts && dojo build --mode test && shx cp -r output fixtures/",
    "build-dist": "dojo build --mode dist",
    "build-dev": "dojo build --mode dev",
    "build-test": "dojo build --mode test"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@dojo/cli": "0.2.0",
    "@dojo/cli-build-app": "file:../dist/dojo-cli-build-app.tgz",
    "tslib": "^1.8.0",
    "intern": "~4.1.0"
  },
  "devDependencies": {
    "shx": "^0.2.2"
  }
}
